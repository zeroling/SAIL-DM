dataset_registry: datasets.yaml
algorithm_config: condense.yaml

project:
  seed: 2026
  deterministic: false
  device: auto
  output_root: outputs/sail_dm
  num_workers: 2
  windows_num_workers: 0
  prefetch_factor: 2
  pin_memory: true
  persistent_workers: true
  amp: true
  amp_dtype: bf16
  matmul_precision: high
